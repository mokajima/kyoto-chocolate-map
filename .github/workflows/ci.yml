name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: 12.13.1
    - run: yarn
    - run: cd functions
    - run: yarn
    - run: cd ../
    - run: yarn lint
